interface:
  display_name: "Python Env Setup"
  short_description: "Create, inspect, and repair uv Python environments, including guarded PyTorch CUDA installs."
  default_prompt: "Use $python-env-setup to set up or repair a uv-managed Python environment and verify the correct CPU or GPU PyTorch configuration."
